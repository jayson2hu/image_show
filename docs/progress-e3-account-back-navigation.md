# E3 个人中心二级页面返回入口进度记录

日期：2026-05-08

## 开发目标

- 用户从个人中心进入图片历史、积分流水、购买中心后，可以明确返回个人中心。
- 默认页保留清晰入口，不让用户进入二级页面后迷路。

## 完成内容

- `/history` 顶部新增“返回个人中心”入口。
- `/credits` 顶部新增“返回个人中心”入口。
- `/packages` 顶部新增“返回个人中心”入口。
- `/account` 默认页已经在 E1 中提供图片历史、积分流水、购买积分、开始生成入口。
- `docs/plan-admin-site-account-ops.md` 进度表已标记 E3 完成。

## 自测记录

- `pnpm.cmd exec vue-tsc --noEmit`：通过。

## 验收结论

E3 局部自测通过，可以提交。
