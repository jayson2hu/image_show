# 管理后台侧边栏折叠进度记录

## 需求

管理后台左边栏增加一个可以缩进去、也可以拉出来的交互。

## 拆分

### 小功能 1：布局层支持侧边栏宽度切换

状态：已完成

改动：

- 在 `AdminLayout.vue` 增加 `sidebarCollapsed` 状态。
- 桌面端布局从 `260px + 内容区` 切换为 `84px + 内容区`。
- 增加宽度切换动画。

自测：

- `pnpm.cmd exec vue-tsc --noEmit`：通过。

### 小功能 2：侧边栏组件支持收起/展开

状态：已完成

改动：

- 在 `AdminSidebar.vue` 增加 `v-model:collapsed`。
- 展开时显示标题、说明、菜单文字和管理员邮箱。
- 收起时显示图标菜单，菜单项保留 `title` 提示。
- 移动端继续使用横向菜单，不引入额外复杂交互。
- 顺手恢复该组件内管理后台文案为正常中文显示。

自测：

- `pnpm.cmd exec vue-tsc --noEmit`：通过。

## 验收标准

- 桌面端管理后台左侧栏可以一键收起：已实现。
- 收起后主内容区域自动变宽：已实现。
- 收起状态下可以通过同一个按钮展开：已实现。
- 菜单切换功能保持可用：已通过类型检查，待页面手动确认。
- 移动端不受影响：移动端模板未改动。

## 问题记录

- 未发现需求矛盾。
